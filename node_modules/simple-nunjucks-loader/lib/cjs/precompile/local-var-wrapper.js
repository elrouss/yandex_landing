"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.localVarWrapper = localVarWrapper;

var _constants = require("../constants");

/**
 * @typedef {Object} NunjucksPrecompiled
 * @property {string} name
 * @property {string} template
 */

/**
 * @param {NunjucksPrecompiled[]} templates
 * @returns {string}
 */
function localVarWrapper(templates) {
  let out = '';

  for (let i = 0; i < templates.length; i++) {
    const {
      name,
      template
    } = templates[i];
    out += `
            ${_constants.TEMPLATE_DEPENDENCIES}.templates[${JSON.stringify(name)}] = (function() {
                ${template}
            })();
        `;
  }

  return out;
}