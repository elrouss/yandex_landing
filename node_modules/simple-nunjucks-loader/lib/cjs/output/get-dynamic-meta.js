"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getDynamicMeta = getDynamicMeta;

var _constants = require("../constants");

function getDynamicMeta(assetPath, assetImport, {
  metaKey,
  importVar,
  depsKey
}) {
  const isDynamicImport = assetImport.isDynamic();
  return `${_constants.TEMPLATE_DEPENDENCIES}.${metaKey}['${depsKey}'] = {
        path: ${isDynamicImport ? assetPath.toRegExp().toString() : JSON.stringify(assetPath.toString())},
        module: ${importVar}
    };`;
}